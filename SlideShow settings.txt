# SlideShow settings

# "Directories:" is followed by the path of the directory holding the photo shows: each
# of its immediate subdirectories is a show, listed with a check box under Select Photo
# Show; every checked show's whole tree is in the slideshow
Directories:
C:\Users\mlo\Documents\usr\Fancyclopedia\Python\SlideShow\Test photos

# Order is Sequential or Random
Order=Sequential

# Display Time is the number of seconds each image is shown
Display Time=4

# Title is shown at the top of the display
Title=photos.fanac.org

# Title Font and Title Font Size set the typeface of the title line (defaults: Segoe UI, 32)
Title Font=#Chicle
Title Font Size=48

# If True, show the subdirectory chain under the title for images not in the top-level directory
Display Subdirectory=True

# Seconds of no user input after which a paused show resumes on its own
Pause Timeout=240

# Mode is Dark or Light (default Dark)
Mode=Dark

# Seconds of no user input after which the remembered email address is forgotten
Email Timeout=60

# How sure the face detector must be before it calls something a face (0 to 1).
# Lower finds more faces, including doubtful ones; higher finds only clear ones.
Face Detection Threshold=0.6
